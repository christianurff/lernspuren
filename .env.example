# Basis-URL des Freigabe-Dienstes (Cloudflare Worker, siehe cloudflare-worker/README.md).
# Ohne eigene Instanz kann „Teilen per Link“ nicht genutzt werden – alle anderen Funktionen laufen rein lokal.
VITE_SHARE_API_URL=https://lernspuren-share.example.workers.dev
